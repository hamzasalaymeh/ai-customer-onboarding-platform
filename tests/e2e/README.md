# End-to-end tests

Placeholder for end-to-end tests that drive the running frontend against a
live backend (e.g. via Playwright). Add a `package.json` here once the
end-to-end framework is chosen, with a `test` script that `tests/README.md`
already documents (`npm install && npm run test`).
